class TimeCard:

    def __init__(self, timeIn, timeOut, work_hours, card_id):
        self._timeIn = timeIn
        self._timeOut = timeOut
        self._card_id = card_id
        self._work_hours = work_hours
